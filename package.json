{
  "name": "cloudflare-ctx-exports-bug-reproduction",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-types": "wrangler types",
    "build": "wrangler types && tsc --noEmit",
    "deploy:version": "wrangler versions upload"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.55.0"
  }
}
